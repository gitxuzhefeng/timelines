{
  "name": "timelens-repo",
  "private": true,
  "description": "TimeLens：本地优先的 macOS/Windows 桌面时间透视 — 被动窗口与应用采集、智能截图、会话聚合（Tauri + React）。应用代码在 project/。",
  "homepage": "https://timelens-pi.vercel.app/",
  "scripts": {
    "dev": "npm run dev --prefix project",
    "build": "npm run build --prefix project",
    "preview": "npm run preview --prefix project",
    "tauri": "npm run tauri --prefix project",
    "tauri:build": "npm run tauri --prefix project -- build",
    "test": "npm run test --prefix project",
    "release": "bash scripts/release.sh",
    "release:macos": "bash scripts/release.sh"
  }
}
